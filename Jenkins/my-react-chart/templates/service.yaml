apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-react-chart.fullname" . }}
  labels:
    {{- include "my-react-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 9999
      protocol: TCP
      name: http
  selector:
    {{- include "my-react-chart.selectorLabels" . | nindent 4 }}
